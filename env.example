## Site URL (used for sitemap, robots.txt, and OpenGraph metadata)
# Set this to your production domain in production
NEXT_PUBLIC_SITE_URL=https://www.lescaducees.fr

## Strapi CMS (server-only; do NOT expose to the browser)
# Base URL of your Strapi instance (include protocol).
# Example: https://cms.example.com
STRAPI_URL=

# Optional: API Token (recommended). `STRAPI_TOKEN` is also accepted as an alias.
# Create one in Strapi Admin → Settings → API Tokens.
STRAPI_API_TOKEN=

# Optional: ISR cache duration (seconds). Default: 300
STRAPI_REVALIDATE_SECONDS=300

# Optional: allow falling back to static demo content when Strapi is misconfigured.
# Recommended: keep disabled in production.
CMS_FALLBACK_TO_STATIC=false

## Strapi Webhooks & Preview Mode
# Secret for on-demand ISR revalidation webhook from Strapi
# Set this to a random string and configure the same value in Strapi webhook settings
STRAPI_WEBHOOK_SECRET=

# Secret for Strapi preview mode (draft content viewing)
# Set this to a random string and configure the same value in Strapi preview settings
STRAPI_PREVIEW_SECRET=
